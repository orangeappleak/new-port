import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['wallpapers-clan.com', 'img.icons8.com', 'sdmntprsouthcentralus.oaiusercontent.com', 'videos.openai.com', 'redport.vercel.app'],
  },
};

export default nextConfig;
